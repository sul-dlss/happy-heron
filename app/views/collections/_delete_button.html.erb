<%= turbo_frame_tag dom_id(collection, :delete) do %>
  <% if allowed_to?(:delete?, collection) %>
    <%= link_to collection_path(collection), method: :delete, aria: { label: "Delete #{collection.head.name}" }, data: { confirm: "Are you sure you want to delete this draft collection? It cannot be undone." } do %>
      <% if params[:style] == 'button' %>
        <span class="btn btn-primary"><span class="far fa-trash-alt"></span> Discard draft</span>
      <% else %>
        <span class="far fa-trash-alt"></span>
      <% end %>
    <% end %>
  <% end %>
<% end %>
