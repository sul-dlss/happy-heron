<div class="container" id="work" data-controller="state-updates" data-state-updates-work-id="<%= @work.id %>">
  <%= render Works::ApprovalComponent.new(work: @work) %>

  <%= render Works::DetailComponent.new(work: @work) %>
</div>
